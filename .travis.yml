language: python
python:
  - "3.6"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: python3 Main.py
before_script:
  - createdb DPSBot
  - psql DPSBot < db-dump/backup.dump -U postgres
